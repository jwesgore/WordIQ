import SwiftUI
import SwiftData

struct StatsView: View {
    
    var body: some View {
        VStack {
            Text("Avg Score")
            Text("Avg Time")
        }
    }
}

#Preview {
    StatsView()
}
